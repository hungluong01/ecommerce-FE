import { MyEcommValidators } from './my-ecomm-validators';

describe('MyEcommValidators', () => {
  it('should create an instance', () => {
    expect(new MyEcommValidators()).toBeTruthy();
  });
});
